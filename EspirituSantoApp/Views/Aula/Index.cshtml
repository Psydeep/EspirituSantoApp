@model IEnumerable<EspirituSantoApp.Models.Aula>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Aula_Num)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Aula_Num)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Aula_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Aula_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Aula_ID })
        </td>
    </tr>
}

</table>
